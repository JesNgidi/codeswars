class Persist 
{
	public static int persistence(long n) {
  if(n / 10 == 0) 
  return 0;
  
 long getMultiplyOfDigits = 1 ;
  for (long i = n; n != 0; n /= 10) {
            getMultiplyOfDigits *= (n % 10);
        }
  
		return persistence(getMultiplyOfDigits) + 1;
	}
}
